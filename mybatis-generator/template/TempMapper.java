package com.jcgroup.${module}.${pkg}.dao.${pkgSub};

import com.hesen.base.BaseMapper;

import org.springframework.stereotype.Repository;
/**
 * ${className}Mapper
 * @author ${author}
 * @since ${createDate}
 */
@Repository
public interface ${className}Mapper<T> extends BaseMapper<T> {
	
	
}
